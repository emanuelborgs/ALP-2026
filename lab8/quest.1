cont = 5
while cont > 0: 
    num = int(input("Digite um número inteiro: "))
    cont -= 1
    # Se o numero for par, o continue faz o loopinfinito
    # voltar para o início sem fazer o print
    # Se o número for ímpar o print será executado.
    if num % 2 == 0: 
        continue
   print{num} e   impar)
